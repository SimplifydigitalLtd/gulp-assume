{
  "name": "gulp-assume",
  "version": "0.0.0",
  "description": "A plugin for Gulp",
  "keywords": [
    "gulpplugin"
  ],
  "author": {
    "name": "",
    "email": "",
    "url": "https://github.com/SteveCarratt"
  },
  "repository": "SteveCarratt/gulp-assume",
  "scripts": {
    "test": "istanbul test _mocha --report html -- test/*.js --reporter spec",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "dependencies": {
    "gulp-util": "~2.2.0",
    "lodash": "^2.4.1",
    "through2": "*"
  },
  "devDependencies": {
    "coveralls": "*",
    "esprima": "^1.2.2",
    "event-stream": "*",
    "istanbul": "*",
    "mocha": "*",
    "mocha-lcov-reporter": "*",
    "should": "^4.6.0"
  },
  "engines": {
    "node": ">=0.8.0",
    "npm": ">=1.2.10"
  },
  "license": "MIT"
}
